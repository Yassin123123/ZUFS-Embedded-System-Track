#include <stdio.h>

int main()
{
    unsigned int x;
    int y, z;
    printf("1-Set bit function.\n2-Clear bit function\n3-Toggle bit function\n4-Read bit function\n");
    printf("Pick the number represents the function you want");
    scanf("%u %d %d", &x, &y, &z);
    if (z == 1)
    {
        for (int i=0; i < y; i++)
    }
    else if (z == 2)
    {

    }
    else if (z == 3)
    {

    }
    else if (z == 4)
    {

    }
}
